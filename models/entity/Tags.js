export class Tags {
    /**
     * @type {Number} the identity record of the Tags table
     */
    id;

    /**
     * @type {Number} the post id
     */
    postId;

    /**
     * @type {String} the Tag name
     */
    tagName;

    /**
     * @type {Boolean} Check if the tag is deleted or not
     */
    isDeleted;
}