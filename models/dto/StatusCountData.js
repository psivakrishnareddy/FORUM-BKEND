/**
 * Contains the Status and its count data
 */
export class StatusCountData {
    /**
     * @type {Number} the identity column of the status
     */
    id;

    /**
     * @type {String} the status name
     */
    status;

    /**
     * @type {Number} the count of data in the specific status
     */
    count;
}