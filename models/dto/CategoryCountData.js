/**
 * Contains the category and its count data
 */
 export class CategoryCountData {
    /**
     * @type {Number} the identity column of the category
     */
    id;

    /**
     * @type {String} the category name
     */
    name;

    /**
     * @type {Number} the count of data in the specific category
     */
    count;
}